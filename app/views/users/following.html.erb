<h1>Following</h1>

  <ul>
  <% @following.each do |f| %>
  <%= image_tag f.image %>
    <h3> <%= link_to "#{f.first_name.capitalize} #{f.last_name.capitalize}", f %></h3>
    <li>Email: <%= f.email %></li>
  <% end %>
  </ul>
